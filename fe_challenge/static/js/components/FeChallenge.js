import React from "react";
import ReactDOM from "react-dom";
// import { FeChallenge } from "./FeChallenge";

export class FeChallenge extends React.Component {
  render() {
    // const p = this.props;
    // return p.messages.map(msg => <Message key={msg.id} message={msg}/>);
    return <p>I am the first component</p>;
  }
}
